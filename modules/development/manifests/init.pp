class development {

   package{ "gcc": status => latest }
   package{ "python-dev": status => latest }
   package{ "meld": status => latest }
   package{ "build-essential": status => latest }
   package{ "python-pip": status => latest }

}
